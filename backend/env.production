# Production Environment (Railway)
NODE_ENV=production
PORT=3001

# MongoDB Atlas Connection (same as local for now)
MONGODB_URI=mongodb+srv://dhiraj30498:RcArMS3FO7QcAiVb@codeveer.lxzs9dq.mongodb.net/codeveer?retryWrites=true&w=majority

# JWT Configuration (use a strong secret in production)
JWT_SECRET=your-super-secret-jwt-key-for-production-change-this
JWT_EXPIRES_IN=7d

# CORS Configuration (will be set to your Vercel frontend URL)
CORS_ORIGIN=https://your-frontend-url.vercel.app 